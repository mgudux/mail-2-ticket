services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
        - SPRING_TASK_EXECUTION_POOL_CORE_SIZE=4
        - SERVER_TOMCAT_CONNECTION_TIMEOUT=5m
